import { SlidesControllerDirective } from './slides-controller.directive';

describe('SlidesControllerDirective', () => {
  it('should create an instance', () => {
    const directive = new SlidesControllerDirective();
    expect(directive).toBeTruthy();
  });
});
